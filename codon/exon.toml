[package]
name = "codon"
version = "0.1.0"
description = "A documentation-site generator (reads book.toml + markdown, emits book.json)."
authors = ["Geunryeol Park (misut)"]
license = "MIT"
type = "bin"
standard = 23

[dependencies]
"github.com/misut/tomlcpp" = "0.3.1"
"github.com/misut/jsoncpp" = "0.1.4"
